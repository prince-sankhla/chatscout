import Link from 'next/link';
import { requireBrand } from '@/lib/brand/authorization';
import { createAdminSupabaseClient } from '@/lib/supabase/admin';

export default async function BrandNotificationsPage(){const brand=await requireBrand();const db=createAdminSupabaseClient() as any;const {data}=await db.from('brand_notifications').select('id,title,message,kind,href,read_at,created_at').eq('user_id',brand.user.id).order('created_at',{ascending:false}).limit(100);return <main className="page-content"><header className="owner-header"><div><Link href="/brand" className="back-link">← Brand dashboard</Link><p className="eyebrow">ACTIVITY</p><h1>Notifications</h1><p>Campaign and application events from your real marketplace activity.</p></div></header><section className="section">{data?.length?<div className="notification-list">{data.map((n:any)=><article key={n.id} className="card"><div className="cardBody"><div className="cardTop"><span className="badge">{n.kind}</span><span className="meta">{new Date(n.created_at).toLocaleString('en-IN')}</span></div><h3>{n.title}</h3><p>{n.message}</p>{n.href&&<Link className="view" href={n.href}>Open →</Link>}</div></article>)}</div>:<div className="empty"><h3>No notifications yet.</h3><p>Application and campaign events will appear here when real activity occurs.</p></div>}</section></main>}
